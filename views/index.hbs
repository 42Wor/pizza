<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>{{title}}</title>
    
    <!-- Favicon (Browser Tab Icon) -->
    <link rel="icon" href="/image/ican.png" type="image/png">

    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- GLOBAL VARIABLES & RESET --- */
        :root {
            --primary: #FF9F1C;
            --bg-dark: #0F1115;
            --bg-card: #181B21;
            --text-white: #ffffff;
            --text-gray: #9ca3af;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Outfit', sans-serif; -webkit-tap-highlight-color: transparent; }
        body { background-color: var(--bg-dark); color: var(--text-white); overflow-x: hidden; padding-bottom: 80px; }

        /* --- NAVBAR --- */
        .navbar {
            position: fixed; top: 0; width: 100%; padding: 10px 5%;
            background: rgba(15, 17, 21, 0.95); backdrop-filter: blur(10px);
            z-index: 100; border-bottom: 1px solid rgba(255,255,255,0.05);
            display: flex; align-items: center; gap: 12px;
        }
        .brand { 
            display: flex; align-items: center; gap: 10px;
            font-weight: 800; font-size: 1.2rem; color: white; text-decoration: none; letter-spacing: 1px; 
        }
        .brand span { color: var(--primary); }
        
        /* Logo Styling */
        .nav-logo-img {
            height: 45px;
            width: 45px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid var(--primary);
        }

        /* --- HERO SECTION --- */
        .hero { margin-top: 85px; padding: 0 5%; margin-bottom: 30px; }
        .hero h1 { font-size: 2rem; line-height: 1.2; }
        .hero p { color: var(--text-gray); margin-top: 5px; }

        /* --- SECTION TITLES --- */
        .section-title { font-size: 1.4rem; font-weight: 800; margin: 0 5% 15px; display: flex; align-items: center; gap: 10px; }
        .section-title i { color: var(--primary); }

        /* --- DEALS CAROUSEL (Horizontal Scroll) --- */
        .deals-wrapper {
            display: flex; overflow-x: auto; gap: 15px; padding: 0 5% 30px;
            scroll-snap-type: x mandatory; -webkit-overflow-scrolling: touch;
        }
        .deals-wrapper::-webkit-scrollbar { display: none; }
        
        .deal-card {
            min-width: 280px; max-width: 320px;
            background: linear-gradient(145deg, #1a1d24, #14161b);
            border-radius: 16px; overflow: hidden; scroll-snap-align: center;
            border: 1px solid rgba(255, 159, 28, 0.3); position: relative; flex-shrink: 0;
        }
        .deal-badge {
            position: absolute; top: 10px; left: 10px; background: #ff4444; color: white;
            padding: 4px 10px; border-radius: 20px; font-size: 0.7rem; font-weight: 800;
        }
        .deal-img { width: 100%; height: 150px; object-fit: cover; }
        .deal-content { padding: 15px; }
        .deal-name { font-size: 1.1rem; font-weight: 700; margin-bottom: 5px; }
        .deal-desc { font-size: 0.8rem; color: #aaa; margin-bottom: 10px; height: 35px; overflow: hidden; }
        .deal-footer { display: flex; justify-content: space-between; align-items: center; }
        .deal-price { font-size: 1.2rem; color: var(--primary); font-weight: 800; }
        .btn-add { background: var(--primary); color: black; border: none; padding: 6px 15px; border-radius: 6px; font-weight: 700; cursor: pointer; }

        /* --- MENU CATEGORIES --- */
        .category-strip {
            position: sticky; top: 65px; z-index: 90; background: var(--bg-dark);
            padding: 10px 5%; display: flex; gap: 10px; overflow-x: auto;
            border-bottom: 1px solid rgba(255,255,255,0.05); margin-bottom: 20px;
        }
        .category-strip::-webkit-scrollbar { display: none; }
        .cat-pill {
            padding: 8px 20px; border-radius: 50px; background: var(--bg-card);
            border: 1px solid rgba(255,255,255,0.1); color: #aaa; font-weight: 600;
            white-space: nowrap; cursor: pointer; transition: 0.2s;
        }
        .cat-pill.active { background: var(--primary); color: black; border-color: var(--primary); }

        /* --- MENU GRID --- */
        .menu-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: 15px; padding: 0 5%; }
        .menu-card {
            background: var(--bg-card); border-radius: 12px; overflow: hidden;
            border: 1px solid rgba(255,255,255,0.05); display: flex; flex-direction: column;
        }
        .menu-img { width: 100%; height: 120px; object-fit: cover; }
        .menu-content { padding: 10px; flex-grow: 1; display: flex; flex-direction: column; }
        .menu-name { font-size: 0.95rem; font-weight: 700; margin-bottom: 5px; }
        .menu-desc { font-size: 0.75rem; color: #777; margin-bottom: 10px; flex-grow: 1; }
        .menu-footer { display: flex; justify-content: space-between; align-items: center; margin-top: auto; }
        .btn-icon { width: 30px; height: 30px; background: rgba(255,255,255,0.1); color: white; border: none; border-radius: 6px; cursor: pointer; display: flex; align-items: center; justify-content: center; }
        .btn-icon:hover { background: var(--primary); color: black; }

        /* --- CART SIDEBAR --- */
        .fab-cart {
            position: fixed; bottom: 25px; right: 25px; width: 60px; height: 60px;
            background: var(--primary); border-radius: 50%; display: flex; align-items: center; justify-content: center;
            box-shadow: 0 10px 20px rgba(255, 159, 28, 0.4); cursor: pointer; z-index: 1000;
        }
        .fab-count {
            position: absolute; top: -5px; right: -5px; background: white; color: black;
            font-weight: 800; font-size: 0.8rem; width: 24px; height: 24px; border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
        }
        .overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8);
            z-index: 1001; opacity: 0; pointer-events: none; transition: 0.3s;
        }
        .overlay.active { opacity: 1; pointer-events: all; }
        .sidebar {
            position: fixed; top: 0; right: -100%; width: 85%; max-width: 400px; height: 100%;
            background: #1a1d24; z-index: 1002; transition: 0.3s; display: flex; flex-direction: column;
        }
        .sidebar.active { right: 0; }
        .sidebar-header { padding: 20px; border-bottom: 1px solid rgba(255,255,255,0.1); display: flex; justify-content: space-between; align-items: center; }
        .sidebar-body { flex-grow: 1; overflow-y: auto; padding: 15px; }
        .cart-item { display: flex; gap: 10px; margin-bottom: 15px; background: rgba(255,255,255,0.05); padding: 8px; border-radius: 8px; align-items: center; }
        .cart-item img { width: 50px; height: 50px; border-radius: 6px; object-fit: cover; }
        .sidebar-footer { padding: 20px; background: #14161b; }
        .input-field { width: 100%; background: #2a2d35; border: none; padding: 12px; border-radius: 8px; color: white; margin-bottom: 10px; }
        .checkout-btn { width: 100%; padding: 14px; background: #25D366; color: white; border: none; border-radius: 8px; font-weight: 700; cursor: pointer; }
        
        /* Toast */
        .toast { position: fixed; top: 20px; left: 50%; transform: translateX(-50%); background: #333; color: white; padding: 10px 20px; border-radius: 30px; z-index: 2000; font-size: 0.9rem; animation: fade 2s forwards; }
        @keyframes fade { 0% { opacity: 0; top: 0; } 20% { opacity: 1; top: 20px; } 80% { opacity: 1; top: 20px; } 100% { opacity: 0; top: 0; } }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar">
        <a href="#" class="brand">
            <!-- Logo Image -->
            <img src="/image/log.jpeg" alt="Logo" class="nav-logo-img">
            JEE BHAI <span>CAFE</span>
        </a>
    </nav>

    <!-- Hero -->
    <div class="hero">
        <h1>Hungry? <br><span style="color:var(--primary)">Order Now.</span></h1>
        <p>Best Fast Food & Deals in Town</p>
    </div>

    <!-- DEALS SECTION -->
    <div class="section-title"><i class="fas fa-fire"></i> Hot Deals</div>
    <div class="deals-wrapper">
        {{#each deals}}
        <div class="deal-card">
            <div class="deal-badge">SAVE MONEY</div>
            <img src="{{this.image}}" class="deal-img" onerror="this.src='https://placehold.co/400x200?text=Deal'">
            <div class="deal-content">
                <div class="deal-name">{{this.name}}</div>
                <div class="deal-desc">{{this.description}}</div>
                <div class="deal-footer">
                    <div class="deal-price">₨{{this.price}}</div>
                    <button class="btn-add" onclick="addToCart({{this.id}})">ADD</button>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    <!-- MENU SECTION -->
    <div class="section-title"><i class="fas fa-utensils"></i> Our Menu</div>
    
    <div class="category-strip">
        <button class="cat-pill active" onclick="filterMenu('all', this)">All</button>
        <button class="cat-pill" onclick="filterMenu('burger', this)">Burgers</button>
        <button class="cat-pill" onclick="filterMenu('pizza', this)">Pizza</button>
        <button class="cat-pill" onclick="filterMenu('fries', this)">Fries</button>
        <button class="cat-pill" onclick="filterMenu('sandwich', this)">Sandwich</button>
    </div>

    <div class="menu-grid" id="menu-grid">
        <!-- Populated by JS -->
    </div>

    <!-- CART UI -->
    <div class="fab-cart" onclick="toggleCart()">
        <i class="fas fa-shopping-bag" style="font-size: 1.4rem; color: black;"></i>
        <div class="fab-count" id="cart-count">0</div>
    </div>

    <div class="overlay" id="overlay" onclick="toggleCart()"></div>
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <h3>Your Order</h3>
            <i class="fas fa-times" style="cursor:pointer; font-size:1.2rem" onclick="toggleCart()"></i>
        </div>
        <div class="sidebar-body" id="cart-items"></div>
        <div class="sidebar-footer">
            <div style="display:flex; justify-content:space-between; margin-bottom:15px; font-weight:800; font-size:1.2rem;">
                <span>Total:</span> <span id="cart-total" style="color:var(--primary)">₨0</span>
            </div>
            <textarea id="address" class="input-field" rows="3" placeholder="Name, Address & Phone..."></textarea>
            <button class="checkout-btn" onclick="checkout()">ORDER ON WHATSAPP</button>
        </div>
    </div>

    <div id="toast-container"></div>

    <script>
        // --- DATA & STATE ---
        const menuData = {{{json menu}}};
        const dealsData = {{{json deals}}};
        const allProducts = [...dealsData, ...menuData];
        let cart = [];

        // --- RENDER MENU ---
        function filterMenu(type, btn) {
            if(btn) {
                document.querySelectorAll('.cat-pill').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
            }

            const grid = document.getElementById('menu-grid');
            grid.innerHTML = '';
            
            const items = type === 'all' ? menuData : menuData.filter(i => i.type.toLowerCase() === type.toLowerCase());

            items.forEach(item => {
                grid.innerHTML += `
                    <div class="menu-card">
                        <img src="${item.image}" class="menu-img" onerror="this.src='https://placehold.co/200?text=Food'">
                        <div class="menu-content">
                            <div class="menu-name">${item.name}</div>
                            <div class="menu-desc">${item.description}</div>
                            <div class="menu-footer">
                                <div style="font-weight:700; color:var(--primary)">₨${item.price}</div>
                                <button class="btn-icon" onclick="addToCart(${item.id})"><i class="fas fa-plus"></i></button>
                            </div>
                        </div>
                    </div>
                `;
            });
        }

        // --- CART LOGIC ---
        function addToCart(id) {
            const item = allProducts.find(p => p.id === id);
            if(item) {
                cart.push(item);
                updateCartUI();
                showToast(`Added ${item.name}`);
            }
        }

        function removeFromCart(index) {
            cart.splice(index, 1);
            updateCartUI();
        }

        function updateCartUI() {
            document.getElementById('cart-count').innerText = cart.length;
            const list = document.getElementById('cart-items');
            const totalEl = document.getElementById('cart-total');
            
            list.innerHTML = '';
            let total = 0;

            if(cart.length === 0) list.innerHTML = '<p style="text-align:center; color:#777; margin-top:20px;">Cart is empty</p>';
            
            cart.forEach((item, index) => {
                total += item.price;
                list.innerHTML += `
                    <div class="cart-item">
                        <img src="${item.image}" onerror="this.src='https://placehold.co/50'">
                        <div style="flex-grow:1">
                            <div style="font-size:0.9rem; font-weight:600;">${item.name}</div>
                            <div style="color:var(--primary); font-size:0.85rem;">₨${item.price}</div>
                        </div>
                        <i class="fas fa-trash" style="color:#ff4444; cursor:pointer" onclick="removeFromCart(${index})"></i>
                    </div>
                `;
            });
            totalEl.innerText = '₨' + total;
        }

        function toggleCart() {
            document.getElementById('sidebar').classList.toggle('active');
            document.getElementById('overlay').classList.toggle('active');
        }

        function checkout() {
            if(cart.length === 0) return showToast('Cart is empty!');
            const address = document.getElementById('address').value;
            if(!address) return showToast('Please enter address details');

            let msg = `*NEW ORDER - JEE BHAI CAFE*\n------------------\n`;
            cart.forEach((item, i) => { msg += `${i+1}. ${item.name} (₨${item.price})\n`; });
            const total = cart.reduce((sum, i) => sum + i.price, 0);
            msg += `------------------\n*Total: ₨${total}*\n\n*Details:*\n${address}`;

            const phone = "923410099882"; 
            window.open(`https://wa.me/${phone}?text=${encodeURIComponent(msg)}`, '_blank');
        }

        function showToast(msg) {
            const div = document.createElement('div');
            div.className = 'toast';
            div.innerText = msg;
            document.getElementById('toast-container').appendChild(div);
            setTimeout(() => div.remove(), 2000);
        }

        // Init
        filterMenu('all');
    </script>
</body>
</html>